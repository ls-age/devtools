{
  "name": "@ls-age/devtools",
  "version": "0.0.0",
  "private": true,
  "description": "A collection of devtools",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ls-age/devtools.git"
  },
  "author": "Lukas Hechenberger <npm@ls-age.com>",
  "homepage": "https://github.com/ls-age/devtools#readme",
  "bugs": {
    "url": "https://github.com/ls-age/devtools/issues"
  },
  "scripts": {
    "format": "prettier --ignore-path .gitignore --plugin-search-dir . \"**/*\" \"!**/{.*ignore,*.png}\"",
    "lint": "eslint --ignore-path .gitignore . --ext .js,.ts",
    "preinstall": "node ./scripts/pnpm-only",
    "sync-metadata": "node ./scripts/sync-metadata",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@ls-age/eslint-config": "0.9.2",
    "@typescript-eslint/eslint-plugin": "2.18.0",
    "@typescript-eslint/parser": "2.18.0",
    "commitlint": "8.3.5",
    "eslint": "6.8.0",
    "eslint-config-prettier": "6.10.0",
    "husky": "4.2.1",
    "lint-staged": "10.0.5",
    "pkg-ok": "2.3.1",
    "prettier": "1.19.1",
    "prettier-plugin-package": "0.3.1",
    "typescript": "3.7.5"
  },
  "commitlint": {
    "extends": [
      "./packages/commitlint-config"
    ]
  },
  "husky": {
    "hooks": {
      "commit-msg": "commitlint -E HUSKY_GIT_PARAMS",
      "pre-commit": "pnpm run sync-metadata && lint-staged"
    }
  },
  "lint-staged": {
    "*.{js,ts,svelte,json,yml,html,sass,scss,css,md}": [
      "prettier --plugin-search-dir . --write"
    ],
    "*.{js,svelte}": [
      "eslint --fix"
    ]
  },
  "renovate": {
    "extends": [
      "@ls-age:automergeDev"
    ]
  }
}
